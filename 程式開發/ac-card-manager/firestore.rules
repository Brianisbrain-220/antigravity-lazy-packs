rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 任何已登入的 Google 帳號即可讀寫所有集合
    // 管理員白名單驗證由前端 AuthContext 處理
    // 未來可改為 custom claims 強化安全性
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
